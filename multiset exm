#include <bits/stdc++.h>

using namespace std;
void print(multiset<string> &s)
{
    for(auto value: s)
    {
        cout<<value<<endl;
    }
}
int main()
{
   multiset<string> s;
  s.insert("abc");//tc o(logn)
   s.insert("zsdf");
   s.insert("bcd");
   s.insert("abc");
   print(s);
  return 0;
}
